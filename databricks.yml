bundle:
  name: forecasting_poc

targets:
  production:
    mode: production
    workspace:
      host: ${var.databricks_host}
    resources:
      jobs:
        Forecasting-POC:
          name: Forecasting-POC

include:
  - databricks/pipelines/*.yml
