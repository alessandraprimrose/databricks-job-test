bundle:
  name: forecasting_poc

targets:
  production:
    mode: production
    workspace:
      host: ${var.databricks_host}
    resources:
      jobs:
        Test_Job_12_05_26:
          name: Test Job - 12/05/26

include:
  - databricks/pipelines/*.yml
