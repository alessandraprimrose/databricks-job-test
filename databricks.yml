bundle:
  name: forecasting_poc

targets:
  production:
    mode: production
    workspace:
      host: ""
    resources:
      jobs:
        Forecasting-POC:
          name: Forecasting-POC

include:
  - databricks/pipelines/*.yml
