bundle:
  name: forecasting_poc

targets:
  production:
    mode: production
    workspace:
      host: ""
      root_path: /Workspace/Users/ally.allen@circyl.co.uk/.bundle/${bundle.name}/${bundle.target}
    resources:
      jobs:
        Forecasting-POC:
          name: Forecasting-POC

include:
  - databricks/pipelines/*.yml
