bundle:
  name: forecasting_poc

targets:
  production:
    mode: production
    workspace:
      host: ${env.DATABRICKS_HOST}
    resources:
      jobs:
        Forecasting-POC:
          name: Forecasting-POC

include:
  - databricks/pipelines/*.yml
